#!/bin/sh

# wallpaper directory
wall_dir="${HOME}/Pictures/Wallpapers"

# visual selection via nsxiv
selected=$(nsxiv -t -r -o "$wall_dir")

# exit if no selection made
[ -z "$selected" ] && exit 0

# set wallpaper and update state file (~/.fehbg)
feh --bg-fill "$selected"
