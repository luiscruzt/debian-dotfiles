#!//bin/sh
# Select a wallpaper visually using nsxiv and set it with feh
WALLPAPERS="${HOME}/Pictures/Wallpapers"

SELECTED=$(nsxiv -t -r -o "$WALLPAPERS")
[ -z "$SELECTED" ] && exit 0
feh --bg-fill "$SELECTED"
